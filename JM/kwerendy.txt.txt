SELECT id, dataWyjazdu, cel, cena FROM `wycieczki` WHERE dostepna = 1;

SELECT nazwaPliku, podpis FROM `zdjecia` WHERE 1
ORDER BY podpis DESC;

SELECT w.cel, w.cena, z.podpis FROM wycieczki w, zdjecia z WHERE z.id = w.zdjecia_id AND w.cena > 1200;

ALTER TABLE wycieczki
ADD COLUMN liczbaDni INT AFTER dataWyjazdu;